# 把这个文件复制成 .env(去掉 .example),填上你自己的 Key。
#
#   .env 在 .gitignore 里,提交不上去 —— 这是唯一该放 Key 的地方。
#   千万不要写进 run.bat:那个文件在版本库里,而这个仓库是公开的。
#
# 改完要重启服务(双击 run.bat)才生效 —— Python 的配置是启动时读的。

OPENAI_API_KEY=sk-在这里粘贴你的key

# 想换模型改这行(默认 gpt-4o-mini)
# RELGRAPH_LLM_MODEL=gpt-4o-mini

# AI 请求最多等多少秒(默认 120)。转圈超过这个时间会直接报错而不是一直等。
# 本地模型出字慢的话可以调大。
# RELGRAPH_LLM_TIMEOUT=120

# AI 录入时把当前圈子**已记录的关系**一并发给模型(默认开)。
# 「我所有朋友都认识她」这类话,"所有朋友"指谁只有你的关系网知道 ——
# 发了模型才能自动展开成具体名单。代价:谁和谁什么关系、多亲近,
# 会随每次 AI 请求发给模型服务商。设为 0 关闭(群体说法会变成一条提示)。
# RELGRAPH_LLM_SEND_RELATIONS=1

# 想让请求走本地模型 / 中转,填这里。留空就是 OpenAI 官方。
# 填了之后,你的文字、截图和当前圈子的人名就不会发到 OpenAI 了。
# OPENAI_BASE_URL=http://127.0.0.1:11434/v1

# ---- 国内网络必看 ----
# api.openai.com 在国内被 DNS 污染 + 封端口,直连必然 "Request timed out"。
# 而 Python 的 openai SDK 底层用 httpx,它**只认下面这两个环境变量**,
# 不读 Windows"设置 → 网络 → 代理"里的开关 —— 那个开关开着也没用。
# 端口改成你自己代理软件的(Clash/Mihomo 默认 7897,v2rayN 默认 10809)。
#
# HTTPS_PROXY=http://127.0.0.1:7897
# HTTP_PROXY=http://127.0.0.1:7897
# NO_PROXY 必须写:不然手机走 Tailscale(100.x 段)的请求和本机自己的
# 请求也会被塞进代理绕一圈。
# NO_PROXY=localhost,127.0.0.1,100.64.0.0/10
